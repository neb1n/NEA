class Colors:
    # Overall background and surfaces
    BACKGROUND = "#f6f7fb"      # very light neutral
    CARD_BG = "#f8fbff"         # soft card background
    HEADER_BG = "#1b3b76"       # soft header band
    MUTED = "#6b6f7a"           # muted text
    TEXT = "#2b2b3a"            # primary text (dark, soft)

    # Accents / CTA
    ACCENT = "#8aa9f7"          # soft pastel blue
    ACCENT_HOVER = "#6f98f0"    # slightly deeper accent for hover states
    CTA_FG = "#ffffff"

    # Seat colours (soft)
    AVAILABLE = "#dff7e6"       # pale green
    SELECTED = "#ffe7b3"        # soft peach (selected)
    RESERVED = "#f7a6a6"        # muted coral (reserved)
    PREMIUM = "#ffe9b3"         # soft amber (premium)

    # Legend / borders
    LEGEND_BORDER = "#000000"